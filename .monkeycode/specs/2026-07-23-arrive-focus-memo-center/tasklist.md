# 抵达 Focus 备忘录中心需求实施计划

- [x] 1. 建立备忘录数据结构和共享契约
  - [x] 1.1 新增 SQLite 备忘录迁移
    - 创建 `memos`、`memo_tags`、`memo_tag_links` 和 `memo_reminders` 表及查询索引
    - 扩展通知投递类型并保留既有记录、唯一约束和外键完整性
    - 需求引用：R2.1-R2.7、R4.1-R4.5、R6.1-R6.13、R8.1
  - [x] 1.2 定义 Rust 领域模型和输入验证
    - 定义备忘录、标签、提醒规则、提醒状态、列表查询和显示标题类型
    - 实现标题、正文、标签数量、标签长度、提醒组合和时区验证
    - 需求引用：R2.6-R2.7、R4.1-R4.3、R6.1-R6.3
  - [x] 1.3 定义 TypeScript DTO 和 command client 接口
    - 创建 `src/features/memos/types.ts` 与 `memoClient.ts`
    - 对齐 `MemoInput`、`MemoRecord`、`MemoSummary`、`MemoListQuery` 和提醒判别联合
    - 需求引用：R1.2、R2.1-R2.7、R3.1-R3.5、R6.1-R6.13
  - [x] 1.4 编写迁移和领域验证单元测试
    - 验证迁移升级、约束、字段边界、时区和提醒字段组合
    - 需求引用：R2.7、R4.3、R6.1-R6.3、R8.1

- [x] 2. 实现备忘录领域服务
  - [x] 2.1 实现创建、读取、更新和显示标题逻辑
    - 规范化输入并维护创建时间、更新时间和置顶时间
    - 实现标题为空时从正文首个非空行生成 40 字符显示标题
    - 需求引用：R2.1-R2.7、R5.1-R5.4
  - [x] 2.2 实现标签规范化和关联替换
    - 按首尾空白和大小写规范化标签，复用已有标签并清理孤立标签
    - 在单次事务中替换备忘录的标签关联
    - 需求引用：R4.1-R4.5
  - [x] 2.3 实现删除事务
    - 在用户确认后的 command 中删除备忘录、标签关联和提醒定义
    - 保持失败事务的数据原子性并返回稳定错误码
    - 需求引用：R7.1-R7.4
  - [x] 2.4 编写 Property M2 标签规范化唯一属性测试
    - 随机生成大小写和空白变体，验证标签记录与关联唯一
    - 需求引用：R4.1-R4.5；设计属性 M2
  - [x] 2.5 编写 Property M6 删除事务属性测试
    - 随机生成关联图并注入 SQL 失败，验证全删或全保留
    - 需求引用：R7.2-R7.4；设计属性 M6

- [x] 3. 实现 SQLite Repository 和列表查询
  - [x] 3.1 实现备忘录 CRUD 与详情聚合
    - 新增 `MemoRepository` 并聚合标签、提醒和审计时间
    - 使用参数化 SQL 和显式事务维护关联数据
    - 需求引用：R1.2、R2.1-R2.7、R4.1-R4.5、R8.1
  - [x] 3.2 实现搜索、标签筛选和稳定排序
    - 对 LIKE 特殊字符执行字面量转义并匹配标题、正文和标签
    - 组合搜索与标签条件，按置顶时间、更新时间和 ID 排序
    - 需求引用：R1.3、R3.1-R3.5、R5.1-R5.3
  - [x] 3.3 实现标签筛选列表
    - 返回仍有关联记录的标签及记录数量
    - 关联变化后保持筛选结果与 SQLite 权威状态一致
    - 需求引用：R3.2-R3.5、R4.4-R4.5
  - [x] 3.4 编写 Property M3 稳定排序属性测试
    - 验证任意记录集合的置顶、更新时间和 ID 排序稳定性
    - 需求引用：R1.3、R5.1-R5.3；设计属性 M3
  - [x] 3.5 编写 Property M8 筛选交集属性测试
    - 验证搜索与标签组合查询的完备性和排他性
    - 需求引用：R3.1-R3.5；设计属性 M8
  - [x] 3.6 编写 Repository 集成测试
    - 覆盖 CRUD、关联替换、孤立标签清理、特殊搜索字符和事务回滚
    - 需求引用：R2.1-R2.7、R3.1-R3.5、R4.1-R4.5、R7.2-R7.4

- [x] 4. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - 运行 Rust 领域与 Repository 测试，确认迁移和核心数据边界可继续集成

- [x] 5. 接入 Tauri commands 和领域事件
  - [x] 5.1 实现备忘录 commands
    - 注册 `memo_list`、`memo_get`、`memo_create`、`memo_update`、`memo_remove` 和 `memo_tag_list`
    - 复用 `CommandResult<T>` 和现有脱敏错误边界
    - 需求引用：R1.1-R1.5、R2.1-R2.7、R3.1-R3.5、R7.1-R7.4
  - [x] 5.2 实现备忘录变更事件
    - 成功写入后发送一次 `memo://changed`，失败写入保持原领域错误
    - 主窗口把事件作为重新读取 SQLite 权威数据的信号
    - 需求引用：R2.2-R2.5、R5.1-R5.4、R9.5
  - [x] 5.3 扩展稳定错误码和双语错误映射
    - 增加备忘录验证、保存、删除和提醒错误码
    - 保持日志脱敏，不记录标题、正文、标签和搜索词
    - 需求引用：R2.5、R4.3、R5.4、R6.11-R6.12、R7.4
  - [x] 5.4 编写 command 与事件接线测试
    - 覆盖参数映射、成功事件、失败隔离和错误文案安全性
    - 需求引用：R2.5、R5.4、R7.4、R9.5

- [x] 6. 实现一次性和重复提醒
  - [x] 6.1 实现提醒规则验证与下一发生时间计算
    - 支持一次、每天、工作日、每周、每月和自定义间隔
    - 处理 IANA 时区、夏令时、开始日期、结束日期和月末收敛
    - 需求引用：R6.1-R6.3、R6.6、R6.9-R6.10
  - [x] 6.2 实现提醒扫描和状态推进
    - 按 `next_scheduled_for` 查询到期提醒并协调投递
    - 一次提醒成功后标记完成，重复提醒成功后计算下一发生时间
    - 需求引用：R6.4、R6.8-R6.10、R6.12-R6.13
  - [x] 6.3 复用通知租约、失败重试和幂等记录
    - 增加 `memoReminder` 投递类型并使用提醒 ID 与发生时间作为唯一身份
    - 处理发布失败、活动 lease、过期接管和已发送推进
    - 需求引用：R6.4、R6.11-R6.13
  - [x] 6.4 实现通知点击打开备忘录
    - 使用只含备忘录 ID 的激活参数显示主窗口
    - 发送 `memo://open-requested`，由页面订阅方定位对应详情
    - 需求引用：R6.4-R6.5、R9.5
  - [x] 6.5 编写提醒规则单元测试
    - 覆盖全部频率、未来时间校验、时区、夏令时和月末日期
    - 需求引用：R6.1-R6.3、R6.6、R6.9-R6.10
  - [x] 6.6 编写 Property M4 提醒发生幂等属性测试
    - 重复协调任意提醒发生时间，验证最多一次成功通知
    - 需求引用：R6.12-R6.13；设计属性 M4
  - [x] 6.7 编写 Property M5 重复提醒单调推进属性测试
    - 验证下一发生时间严格递增并符合本地规则
    - 需求引用：R6.2-R6.3、R6.9-R6.10；设计属性 M5
  - [x] 6.8 编写通知 worker 集成测试
    - 覆盖失败重试、lease 接管、进程中断恢复、点击事件和权限缺失
    - 需求引用：R6.4-R6.13

- [x] 7. 扩展备份格式和恢复事务
  - [x] 7.1 将备份格式升级为版本 2
    - 导出备忘录、标签、关联和提醒集合
    - 保留版本 1 解析能力并为空集合提供确定转换
    - 需求引用：R8.2-R8.5
  - [x] 7.2 实现版本 2 预校验和恢复
    - 校验字段、枚举、时区、唯一性和跨记录引用
    - 按外键顺序在现有恢复事务中替换备忘录相关数据
    - 需求引用：R8.2-R8.5
  - [x] 7.3 编写 Property M7 备份往返属性测试
    - 随机生成有效备忘录关系图并验证导出、解析、恢复和再导出等价
    - 需求引用：R8.2-R8.5；设计属性 M7
  - [x] 7.4 编写备份兼容与故障集成测试
    - 覆盖版本 1 导入、版本 2 引用损坏、恢复成功和事务回滚
    - 需求引用：R8.2-R8.5

- [x] 8. 实现备忘录页面信息架构和响应式骨架
  - [x] 8.1 增加左侧导航和页面路由状态
    - 在“今日”和“项目”之间增加备忘录入口、图标和当前页标题
    - 接入 `memo://changed` 与 `memo://open-requested` 生命周期
    - 需求引用：R1.1、R6.5、R9.5
  - [x] 8.2 实现桌面双栏布局
    - 创建 360px 列表栏与弹性编辑栏，复用现有 Panel 和语义令牌
    - 为列表与编辑器提供独立滚动边界和稳定空白处理
    - 需求引用：R1.2-R1.5、R9.1、R9.4
  - [x] 8.3 实现窄窗口单栏布局
    - 在内容宽度小于 980px 时切换列表与详情视图
    - 提供可读的返回列表操作并保持当前筛选状态
    - 需求引用：R1.5、R9.3-R9.4
  - [x] 8.4 实现加载、空、零结果和失效状态
    - 按 UI 设计状态表实现骨架、主行动、清除条件和权威刷新
    - 需求引用：R1.4、R3.4-R3.5、R9.1
  - [x] 8.5 编写页面骨架与响应式组件测试
    - 覆盖导航、双栏、单栏、空状态、零结果和记录失效
    - 需求引用：R1.1-R1.5、R3.4-R3.5、R9.4

- [x] 9. 实现备忘录列表、搜索、标签和置顶交互
  - [x] 9.1 实现列表项和状态摘要
    - 展示显示标题、两行正文摘要、最多三个标签、提醒摘要和更新时间
    - 使用图标与文字共同表达置顶和提醒状态
    - 需求引用：R1.2-R1.4、R5.1-R5.3、R6.8-R6.9
  - [x] 9.2 实现搜索与标签筛选交互
    - 使用 200 毫秒防抖提交搜索，支持标签选择、交集查询和清除条件
    - 在主窗口生命周期内恢复最近搜索词和标签筛选
    - 需求引用：R1.5、R3.1-R3.5
  - [x] 9.3 实现标签输入和置顶操作
    - 支持标签创建、复用、移除、数量限制和可访问状态提示
    - 保存置顶状态并根据权威列表更新位置
    - 需求引用：R4.1-R4.5、R5.1-R5.4
  - [x] 9.4 编写列表交互单元测试
    - 覆盖摘要、搜索防抖、筛选恢复、标签上限和置顶失败回滚
    - 需求引用：R1.2-R1.5、R3.1-R3.5、R4.1-R4.5、R5.1-R5.4

- [x] 10. 实现编辑器、自动保存和删除确认
  - [x] 10.1 实现标题和纯文本正文编辑器
    - 提供字符限制、保存状态、显式保存和 Ctrl+Enter
    - 新建草稿在首次有效保存后转为权威记录
    - 需求引用：R2.1-R2.3、R2.6-R2.7
  - [x] 10.2 实现防竞态自动保存
    - 停止输入 500 毫秒后保存并区分草稿值、保存版本和权威回写
    - 保存失败时保留草稿并提供重新保存
    - 需求引用：R2.2-R2.5
  - [x] 10.3 实现确认删除对话框
    - 显示备忘录标题和删除影响，确认后调用事务删除
    - 取消与失败均保持记录和当前编辑上下文
    - 需求引用：R7.1-R7.4
  - [x] 10.4 编写 Property M1 最新草稿保持属性测试
    - 生成交错输入与保存完成顺序，验证最终显示最后输入值
    - 需求引用：R2.2-R2.5；设计属性 M1
  - [x] 10.5 编写编辑器组件测试
    - 覆盖新建、限制、自动保存、快捷保存、失败重试和删除确认
    - 需求引用：R2.1-R2.7、R7.1-R7.4

- [x] 11. 实现提醒编辑器 UI
  - [x] 11.1 实现一次与重复提醒 Dialog
    - 使用分段控件切换提醒类型并渐进展示对应字段
    - 支持日期、时间、频率、间隔、星期、月日期、时区和结束日期
    - 需求引用：R6.1-R6.3、R6.6-R6.7
  - [x] 11.2 实现提醒摘要、验证和状态反馈
    - 显示明确的下一提醒时间、重复摘要、已提醒和通知权限说明
    - 支持修改、取消和字段级错误聚焦
    - 需求引用：R6.4、R6.6-R6.11
  - [x] 11.3 编写提醒编辑器组件测试
    - 覆盖全部频率、字段渐进显示、未来时间验证、修改和取消
    - 需求引用：R6.1-R6.11

- [x] 12. 完成国际化、无障碍和视觉一致性
  - [x] 12.1 补齐简体中文和英文资源
    - 增加导航、空状态、表单、提醒、确认、Toast 和错误文案
    - 验证两套资源键完整一致
    - 需求引用：R6.4-R6.5、R9.2
  - [x] 12.2 应用现有设计令牌和组件规范
    - 实现 `.memo-*` 样式命名空间、4.5:1 对比度和减少动态效果适配
    - 保持 40px 操作目标、Panel 圆角、状态色和保存反馈一致
    - 需求引用：R9.1、R9.4
  - [x] 12.3 完成键盘导航和焦点管理
    - 实现搜索、标签、列表、新建、标题、置顶、正文、提醒、删除和保存的连续顺序
    - 为图标按钮提供可读名称和 tooltip，Dialog 关闭后恢复触发点焦点
    - 需求引用：R9.3-R9.4
  - [x] 12.4 编写国际化与无障碍契约测试
    - 覆盖资源完整性、可读名称、焦点顺序、125% 文本缩放和减少动态效果
    - 需求引用：R9.1-R9.4

- [x] 13. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - 运行前端测试、类型检查、生产构建、Rust 格式、测试、desktop-app 编译、严格 Clippy 和补丁检查
