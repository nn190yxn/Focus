{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window access to Arrive Focus domain commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
